// This module will be used when data-controller === 'boilerplate'

const boilerplate = {
    init() {
        // Called automatically
    },
    action() {
        // Called when data-action === 'action'
    }
};

module.exports = boilerplate;